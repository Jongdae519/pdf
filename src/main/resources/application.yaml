spring:
  application:
    name: offline-forge
  servlet:
    multipart:
      max-file-size: 50MB      # 업로드하는 파일 1개당 최대 허용 크기
      max-request-size: 1000MB  # 1번의 HTTP 요청(여러 파일 포함 가능)에 대한 최대 허용 크기

server:
  port: 13500